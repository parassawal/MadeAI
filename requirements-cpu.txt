# CPU-Compatible Requirements (macOS/No GPU)
# Simplified dependencies for running without CUDA

# Core libraries
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Model export and conversion
sentencepiece>=0.1.99
protobuf>=3.20.0

# Utilities
tqdm>=4.65.0

# Note: Unsloth, bitsandbytes, and xformers are removed
# as they require CUDA. Use this for CPU-only environments.
